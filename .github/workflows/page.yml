name: Build Vue
on: [push]
jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Vue
    steps:
    - uses: actions/checkout@v2
    - id: Build-Vue
      uses: KrisCris/VuePagesAction@1.0.0
      with:
        username: 'csex57'
        reponame: 'causeofdeath'
        token: ${{ secrets.GITHUB_TOKEN }}
        base: 'causeofdeath'
