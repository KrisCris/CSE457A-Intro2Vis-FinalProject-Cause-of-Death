name: Build Vue
on: [push]
jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Vue
    steps:
    - uses: actions/checkout@v2
    - id: Build-Vue
      uses: csex57/causeofdeath/.github/workflows/buildVue.yml
      with:
        username: 'csex57'
        reponame: 'causeofdeath'
        token: ${{ secrets.GITHUB_TOKEN }}
        base: 'causeofdeath'
